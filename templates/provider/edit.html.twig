{% extends 'base.html.twig' %}

{% block title %}Modifică furnizor{% endblock %}

{% block body %}
    <h1>
        Modifică furnizor
        <small><a href="{{ path('provider_index') }}">înapoi la listă</a></small>
    </h1>

    {{ form_start(form) }}
    {{ include('provider/_form.html.twig') }}

    <p class="text-right">
        <button class="btn btn-primary">{{ 'Update'|trans }}</button>
    </p>
    {{ form_end(form) }}

    <div class="mt-n5">
        {{ include('provider/_delete_form.html.twig') }}
    </div>
{% endblock %}
