{% extends 'base.html.twig' %}

{% block title %}MerchandiseCategory{% endblock %}

{% block body %}
    <h1>MerchandiseCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ merchandise_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ merchandise_category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('merchandise_category_index') }}">back to list</a>

    <a href="{{ path('merchandise_category_edit', {'id': merchandise_category.id}) }}">edit</a>

    {{ include('merchandise_category/_delete_form.html.twig') }}
{% endblock %}
