{% extends 'base.html.twig' %}

{% block title %}Balance{% endblock %}

{% block body %}
    <h1>Balance</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ balance.id }}</td>
            </tr>
            <tr>
                <th>Amount</th>
                <td>{{ balance.amount }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ balance.date ? balance.date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('balance_index') }}">back to list</a>

    <a href="{{ path('balance_edit', {'id': balance.id}) }}">edit</a>

    {{ include('balance/_delete_form.html.twig') }}
{% endblock %}
