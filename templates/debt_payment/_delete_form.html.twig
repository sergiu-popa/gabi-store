<form method="post" action="{{ path('debt_payment_delete', {'id': debt_payment.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ debt_payment.id) }}">
    <button class="btn">Delete</button>
</form>
