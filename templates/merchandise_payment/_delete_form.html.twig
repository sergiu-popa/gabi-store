<form method="post" action="{{ path('merchandise_payment_delete', {'id': merchandise_payment.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ merchandise_payment.id) }}">
    <button class="btn">Delete</button>
</form>
